Yelp_review dataset: reviews etc from yelp 

Train a model with SFTTrainer (SFT = supervised fine tuning)

Hugging Face is an open-source ML platform with a built-in transformers library
    built-in datasets can be loaded using the load_dataset function

Tokenizer function extracts the text and applies the tokenizer

num_labels parameter specifies number of classes and determines number of neurons



SFT Trainer:
    Simplifies and automates many training tasks
    Efficient and less error-prone
    Parameters include:

        Model

        Training arguments

        Dataset

        Specific field key